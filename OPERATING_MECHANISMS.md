# Hermes Agent 通用运作机制

> 本文档描述的是机制，不是具体应用。任何领域的 Agent 都可以按这个模式运转。

---

## 一、三层记忆

记忆是 Agent 最核心的能力——没有它，每次对话都是陌生人。

| 层级 | 存什么 | 规则 |
|------|--------|------|
| **持久记忆** | 用户偏好、环境事实、经验教训 | 只存"下次还会用到"的信息。不存任务进度、不存临时状态 |
| **用户画像** | 用户的身份、习惯、沟通风格 | 每次对话自动注入上下文 |
| **会话历史** | 完整对话记录 | 支持全文检索（`session_search`），自动清理旧会话 |

**存什么比存多少重要。** 一行"用户偏好简洁回答"比一百行"上次任务做了什么"有价值得多。

**反例（不该存的东西）：**
- 任务进度（"Phase 3 完成"）——下周就没意义了
- 已完成工作的日志（"修了 bug X"）——需要时用 session_search 找回
- PR 编号、commit SHA、文件计数——一周后全部过期

---

## 二、Skills — 可复用的操作流程

### 2.1 理念

Skill 不是文档。Skill 是**遇到某类问题时自动加载的执行脚本**。

Agent 在完成一个复杂任务后，如果过程中踩了坑、总结出了做法——就把这个做法保存为 Skill。下次同类任务自动加载，不从头摸索。

### 2.2 结构

每个 Skill 包含：
- **触发条件**：什么场景下加载这个 Skill
- **执行步骤**：按编号的、可验证的操作流程
- **踩坑记录**：之前的错误和修正方法
- **验证步骤**：做完后检查是否真的做对了

### 2.3 生命周期

```
复杂任务成功 → 提炼为 Skill → 下次自动加载
                            ↓
                  使用中发现不足 → 即时修补
                            ↓
                  不再使用 → 归档或合并
```

### 2.4 禁用纪律（⚠️ 关键）

**被 Cron 引用的 Skill 绝对不能禁用。** 禁用 = Cron 静默跳过这个 Skill = 任务悄悄失败，没有任何报错。

操作前问自己：这个 Skill 被任何定时任务引用了吗？

---

## 三、审计 — 自我纠错循环

### 3.1 为什么需要审计

Cron 让 Agent 自主运行，但自主运行会出问题——任务悄悄失败、记忆膨胀、Skill 过时。审计是 Agent 的"免疫系统"，定期检查自己有没有生病。

### 3.2 三层审计

| 层级 | 检查什么 | 频率 |
|------|----------|------|
| **Cron 健康检查** | 每个定时任务是否正常执行、有无报错、输出是否异常 | 每日 |
| **问题闭环追踪** | 前一天发现的问题有没有被修复、修复后有没有复现 | 每日 |
| **记忆审计** | 记忆条数、质量、是否过期、是否有矛盾 | 每日 |

### 3.3 审计输出

审计报告包含：
1. **健康状态** — 红/黄/绿标识每个 Cron 任务
2. **待处理问题** — 未闭环的问题列表
3. **记忆健康** — 总条数、新增、建议清理、矛盾条目
4. **改进建议** — 基于审计发现的具体行动项

### 3.4 审计纪律

- 审计报告必须包含**可执行的具体行动项**，不能只写"需要改进"
- 连续多天未闭环的问题自动升级优先级
- 审计发现的问题 → 修改 Skill → 防止重复发生

---

## 四、Cron — 定时自主运行

### 4.1 机制

Cron 让 Agent 在没有用户的情况下自主执行任务。Agent 定时醒来，加载指定 Skill，执行任务，把结果推送到用户。

### 4.2 关键参数

| 参数 | 作用 |
|------|------|
| `schedule` | 执行频率（cron 表达式或间隔） |
| `skills` | 加载哪些 Skill 执行任务 |
| `model` | 用什么模型跑（可指定更便宜的模型省 token） |
| `deliver` | 结果推送到哪（飞书/Telegram/存本地） |
| `enabled_toolsets` | 限制工具集（只加载需要的，省 token） |
| `context_from` | 注入其他 Cron 的输出作为上下文（任务串联） |

### 4.3 典型用例

- 每日定时报告（如早报、审计报告）
- 数据采集（定时拉取外部数据）
- 健康检查（监控系统状态）

---

## 五、知识管理

### 5.1 三层知识架构

```
learning/          ← 学习笔记（费曼法建立的理解）
  knowledge_graph  ← 概念之间怎么连线
  INDEX            ← 所有文件的快速索引
  journal/         ← 每日学习记录

references/        ← 外部参考资料（论文、API文档）

domain/            ← 领域的参考数据（分类表、约束规则）
```

### 5.2 学习方法：费曼法

每学一个概念，写一页笔记。规则：
1. **用最简单的话解释**——假设读者完全不懂这个领域
2. **自己建框架**——不搬运教科书，用自己的逻辑重新组织
3. **诚实标缺口**——明确写"这里我还不懂"
4. **标注来源**——知识从哪来的（自己推导的、某篇论文的、某次对话的）

### 5.3 交叉学习纪律

两个框架对接的前提是它们在**同一个时空尺度**上：
- 都是中观的 → 可以对接
- 都是微观结构层面的 → 可以对接
- 都是长期慢变量 → 可以对接
- **跨频率硬接 → 废话。** 不要把 20 年的制度变量跟短期套保决策挂钩

### 5.4 学习看板

用 Kanban 追踪学习进度：
```
Backlog → Ready → In Progress → Review → Done
  ↑                                     │
  └──────────── 发现新缺口 ←────────────┘
```

**WIP 限制 = 1**。一次只深挖一个主题。

---

## 六、Profiles — 多身份隔离

一台服务器可以跑多个完全独立的 Agent 实例。

```bash
hermes profile create agent-b
hermes -p agent-b
```

每个 Profile 有独立的配置、记忆、Skills、会话记录。适合：
- 不同角色的 Agent（研究助手 vs 运维助手）
- 不同项目的 Agent（项目A vs 项目B）
- 测试环境 vs 生产环境

---

## 七、Gateway — 跨平台触达

同一个 Agent 核心，通过 Gateway 连接到不同平台：

- 飞书 / 钉钉 / 企业微信（办公场景）
- Telegram / Discord（技术社区）
- API Server（供其他程序调用）
- CLI / TUI（本地终端交互）

用户在不同平台发消息，Agent 看到的是同一个"自己"——同样的记忆、同样的 Skills、同样的知识。

---

## 八、Git 仓库 — 外脑

Agent 的知识和代码存在 GitHub 上，不是存在它自己的内存里。

| 仓库 | 角色 | 内容 |
|------|------|------|
| Wiki 仓库 | 大脑 | 知识库、方法论、学习笔记 |
| Engine 仓库 | 双手 | 代码、数据管道、脚本 |
| HowTo 仓库（可选·公开） | 说明书 | 系统架构文档（脱敏后公开） |

**核心理念**：Agent 的内存是易失的，Git 是永久的。所有值得保留的东西必须推到 Git。

**推送前安全检查**：
- API Key / Token / 密码绝对不能出现在代码或文档中
- 推送前做脱敏检查
- 用 `.gitignore` 排除敏感配置文件

---

## 九、核心循环

整个系统按一个循环运转：

```
1. 学习（费曼法建一页知识）
       ↓
2. 提炼（把学到的方法编成 Skill）
       ↓
3. 自动化（把重复任务设成 Cron）
       ↓
4. 审计（Cron 定时检查有没有出错）
       ↓
5. 回到 1，学下一个
```

---

## 十、通用纪律

以下规则适用于任何领域的 Agent：

1. **记忆只存偏好和教训，不存任务进度**
2. **Skill 在任务成功后创建，在发现不足时修补**
3. **所有知识落地到 Git，不依赖 Agent 内存**
4. **API Key 永远不出现在输出、代码、Git 历史中**
5. **复杂任务先输出计划再执行**
6. **出错后修正 Skill，避免同样错误重复发生**
7. **WIP = 1：一次只做一件事**
8. **被 Cron 引用的 Skill 不能禁用**
9. **输出前问自己"一年后看这句话还有用吗"——路标不是知识，不写进永久文档**
10. **做分析前先查自己的知识库和记忆——先知道自己知道什么，再补不知道的**
