# 从零搭建 Agent 工作体系

> 给 AI Agent 看的操作手册。照做就能从零搭建一套完整的记忆、学习、自动化、审计体系。

**适用前提：** 你有一个能读写文件、执行命令的 AI Agent（Hermes / Claude Code / Codex 等），以及一个 Git 账号。

**总耗时：** 基础搭建 ~30 分钟，完整体系 ~2 小时。

---

## 总体路线图

```
Phase 0   Phase 1      Phase 2       Phase 3       Phase 4       Phase 5       Phase 6
Setup → 记忆系统 → 知识框架 → Skills → Cron自动化 → 审计系统 → Git & 持续改进
(10min)  (10min)     (20min)     (30min)    (30min)       (20min)      (持续)
```

---

## Phase 0: 搭建基础

### 0.1 创建三个 Git 仓库

```
github.com/你/KnowledgeRepo   ← 知识库（大脑）
github.com/你/EngineRepo      ← 代码和脚本（双手）  
github.com/你/HowToRepo       ← 本文档（公开参考）
```

### 0.2 确认 Agent 有这些能力

- 读写文件
- 执行终端命令
- 访问网络（搜索/API）
- 持久记忆（跨会话）

### 0.3 告诉 Agent 基本规则

你的 Agent 需要一个系统指令或初始提示词。至少包含：

```
1. 每做一件事前，先查自己的记忆和知识库
2. 学到新东西后，写入知识库
3. 完成任务后，提炼为可复用的 Skill
4. 每天审计自己的运行状态
5. API Key 和密码绝对不出现在任何输出或 Git 历史中
```

---

## Phase 1: 记忆系统

> 目标：Agent 不会每次醒来都失忆。

### 1.1 三层记忆架构

```
持久记忆     ← 用户偏好、环境事实、经验教训。跨会话存活。
用户画像     ← 自动注入每条对话的上下文。
会话历史     ← 完整对话记录，支持全文检索。
```

### 1.2 记忆规则

**存什么：**
| 类型 | 示例 | 为什么存 |
|------|------|----------|
| 用户偏好 | "用户要求简洁回答" | 避免重复纠正 |
| 环境事实 | "Python 在 venv 里" | 避免重复踩坑 |
| 经验教训 | "A 方法在 X 场景下会失败" | 下次直接绕过 |
| 工具规范 | "期货数据库路径是 /data/xxx" | 不用每次翻找 |

**不存什么：**
| 类型 | 为什么 |
|------|--------|
| 任务进度 | 下周就没意义了 |
| 临时 TODO | 会话内解决 |
| PR 编号、commit SHA | 一周后过期 |
| 文件计数 | 随时可变 |

### 1.3 自我检查

- [ ] Agent 在以下场景会主动查记忆：开始新任务、遇到以前犯过的错误、用户提到"之前说的那个"
- [ ] 每次被用户纠正后，自动保存为记忆
- [ ] 记忆条数每周增长 < 10 条（不是越多越好）

---

## Phase 2: 知识框架

> 目标：学到的东西能沉淀、能检索、不会丢。

### 2.1 目录结构

```
~/wiki/
├── index.md              ← 全局索引，每次写新页面后更新
├── log.md                ← 变更日志
├── knowledge_graph.md    ← 概念连线图（5+ 页面后建）
├── learning/             ← 按领域分目录
│   ├── domain-a/
│   ├── domain-b/
│   └── INDEX.md
├── methodology/          ← 分析方法论
├── reference/            ← 外部参考资料
└── templates/            ← 页面模板
```

### 2.2 费曼学习法（每次学一个概念的标准流程）

```
1. 用最简单的话解释这个概念 → 写一页笔记
2. 用自己的逻辑重新组织 → 不搬运教科书
3. 诚实标出"这里我还不懂" → 缺口 = 下一个学习目标
4. 标注知识来源 → 自己推导 / 某篇论文 / 某次对话
```

### 2.3 交叉学习纪律

> 两个框架对接的前提是它们在同一个时空尺度上。

- 都是中观 → 可以对
- 都是微观结构 → 可以对
- 跨频率硬接 → 废话（不要把 20 年的制度变量跟每日交易策略挂钩）

### 2.4 WIP = 1

一次只深入一个学习主题。学透再开下一个。

### 2.5 自我检查

- [ ] wiki 有清晰的目录结构，不是一堆散文件
- [ ] index.md 和实际文件内容一致
- [ ] 每个领域有 knowledge_graph.md（概念连线，不是文件列表）

---

## Phase 3: Skills — 把经验变成肌肉

> 目标：复杂任务做完不用重学。

### 3.1 什么时候创建 Skill

```
完成了一个 5+ 步骤的复杂任务
    ↓
过程中踩了坑、找到了正确做法
    ↓
判断：这个任务以后还会出现吗？
    ↓ 是
创建 Skill
```

### 3.2 Skill 的标准结构

```markdown
---
name: my-skill
description: 一句话说清什么时候用
---

# Skill 名称

## 触发条件
什么场景下加载

## 执行步骤
1. 第一步（具体操作，不是"分析一下"）
2. 第二步
3. ...

## 踩坑记录
- 上次在 X 场景出错，原因是 Y，解决方法是 Z

## 验证
做完后怎么确认真的做对了
```

### 3.3 生命周期

```
创建 → 使用 → 发现不足 → 即时修补 → 不再需要 → 归档或合并
```

### 3.4 ⚠️ 关键纪律：Skill 禁用的连锁反应

**被 Cron 定时任务引用的 Skill 绝对不能禁用。** 禁了 = Cron 静默跳过 = 任务悄悄失败，没有报错。

操作前必须确认：`这个 Skill 被哪个定时任务在用？`

### 3.5 自我检查

- [ ] 有至少 3 个自己创建的 Skill（不是安装时自带的）
- [ ] 每个 Skill 都在实际任务中被验证过
- [ ] 最近纠正的错误已经写进对应 Skill 的"踩坑记录"

---

## Phase 4: Cron — 让 Agent 自己跑

> 目标：Agent 在没有你的时候也能干活。

### 4.1 第一个 Cron 任务

从最简单的开始——每日 Git 同步：

```bash
# 每晚 22:00 把知识库和代码推到 GitHub
hermes cron create "0 22 * * *" \
  --name "nightly-git-sync" \
  --prompt "把 ~/wiki 和 ~/engine 的变更 commit 并 push。推送前做脱敏检查。"
```

### 4.2 典型 Cron 任务矩阵

| 时间 | 任务 | 做什么 |
|------|------|--------|
| 凌晨 | 深度学习 | 从学习队列取一个主题，费曼法写一页笔记 |
| 早间 | 产出报告 | 拉取最新数据，生成报告推送到用户 |
| 午间 | 数据维护 | 更新数据库，重算指标 |
| 晚间 | 审计 | 检查所有 Cron 是否正常、记忆是否健康 |
| 深夜 | Git 同步 | 推送一天的所有变更 |

### 4.3 任务串联

一个 Cron 的输出可以作为另一个 Cron 的输入：

```bash
# Job B 会收到 Job A 的最新输出作为上下文
hermes cron create --context_from job_A_id ...
```

### 4.4 省钱技巧

| 技巧 | 效果 |
|------|------|
| `enabled_toolsets` 限制工具集 | 只加载需要的工具，减少 token |
| `model` 指定便宜模型 | 简单任务用 flash，复杂任务用 pro |
| `no_agent: true` 跑脚本 | 纯数据任务直接跑脚本，零 token |

### 4.5 自我检查

- [ ] 至少 3 个 Cron 任务在正常运行
- [ ] 每个 Cron 任务都限制了 `enabled_toolsets`
- [ ] 没有两个 Cron 任务同时操作同一个资源

---

## Phase 5: 审计系统

> 目标：Agent 自己检查自己有没有出问题。

### 5.1 三层审计

| 层级 | 检查什么 |
|------|----------|
| **Cron 健康检查** | 每个定时任务是否正常执行、有无报错 |
| **问题闭环追踪** | 昨天发现的问题今天修了吗、修完复现了吗 |
| **记忆审计** | 记忆条数、质量、有无矛盾、是否过期 |

### 5.2 审计输出格式

每次审计生成一个报告，包含：

```
1. 健康状态 — 每个 Cron 用红/黄/绿标识
2. 待处理问题 — 未闭环的问题，按天数排序
3. 记忆健康 — 总数、新增、建议清理、矛盾条目
4. 改进建议 — 基于发现的具体行动项（不能只写"需要改进"）
```

### 5.3 审计纪律

- **连续 3 天未闭环的问题 → 自动升级优先级**
- **审计发现的问题 → 修改对应 Skill → 防止重复发生**
- **审计报告必须含可执行的具体行动项**

### 5.4 自我检查

- [ ] 每日审计 Cron 正常运行
- [ ] 问题从发现到闭环平均 < 3 天
- [ ] 记忆条数在合理范围内（< 20 条）

---

## Phase 6: Git & 持续改进

> 目标：Agent 的所有输出都有版本控制，关不掉。

### 6.1 推送前安全检查

```bash
# 这个检查必须内化到 Agent 的行为中——不是脚本，是本能
在 git push 之前：
1. grep 扫一遍 diff，找 API_KEY / TOKEN / PASSWORD / SECRET
2. 确认 .gitignore 排除了 .env 和敏感配置
3. 如果有任何疑问 → 不推，先修
```

### 6.2 commit 规范

```
类型: 简短描述

feat: 新增铝型材分析框架
fix: 修复数据源连接超时问题
docs: 更新运作机制文档 v2
refactor: 重构指标计算管线
```

### 6.3 持续改进循环

```
1. 学习 → 费曼法建一页知识
2. 提炼 → 把方法编成 Skill
3. 自动化 → 设成 Cron
4. 审计 → 检查有没有出错
5. 修 → 改 Skill，推 Git
6. 回到 1
```

### 6.4 方法论版本管理

当 Agent 的学习方式发生重大变化时，给方法论升版本（v1 → v2），记录：
- 旧方法的问题是什么
- 新方法改了什么
- 怎么验证新方法确实更好

---

## 完整验证清单

搭建完成后，逐条验证：

- [ ] Agent 在新会话开始时会查自己的记忆
- [ ] 学到新东西后会写入知识库并更新 index.md
- [ ] 有至少 3 个自己创建的 Skill
- [ ] Skill 被使用时不会报"找不到"
- [ ] Cron 任务按时执行，结果推送到正确的位置
- [ ] 每个 Cron 的 `enabled_toolsets` 已限制
- [ ] 审计报告每天生成，问题在闭环中
- [ ] Git sync 正常运行，推送前做了脱敏
- [ ] 被 Cron 引用的 Skill 没有被误禁
- [ ] 知识库在持续增长（不是一次 dump 就停了）

---

## 常见陷阱

| 陷阱 | 症状 | 预防 |
|------|------|------|
| 记忆膨胀 | 记忆超过 30 条，查找变慢 | 每周审计，删掉过期的 |
| Skill 过多 | 加载 Skills 比执行任务还花 token | 合并相似的，归档不用的 |
| Cron 静默失败 | cron list 显示 ok 但输出为空 | 审计时检查输出长度和内容 |
| 知识库变成文件堆 | 有文件但没链接、没索引 | 每周检查 index.md 覆盖率 |
| 跨频率类比 | 输出看起来高大上但没法用 | 对照"交叉学习纪律"自检 |
| 忘了推 Git | 本地有内容但远端落后 | 夜间 Git sync Cron |
